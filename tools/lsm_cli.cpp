// CLI tool stub — will eventually let you interact with the database
// from the terminal (like redis-cli or sqlite3).
//
// #include <cstdio> gives us std::printf(). The "c" prefix means it's
// a C standard library header wrapped for C++. printf() writes formatted
// text to stdout (the terminal).
//
// Every C++ program needs a main() function — it's the entry point,
// like the top-level code in a Node.js script. The return value (0)
// tells the OS the program succeeded (non-zero = error).
#include <cstdio>

int main() {
  std::printf("lsm-cli: not yet implemented\n");
  return 0;
}
